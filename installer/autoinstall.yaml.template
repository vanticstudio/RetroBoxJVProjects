#cloud-config
# >>> TEMPLATE-ONLY >>> (this block is removed when the answer file is rendered)
#
# Retro Box unattended installer - TEMPLATE. JV Projects.
#
# THIS FILE IS A TEMPLATE AND IT IS COMMITTED TO A PUBLIC REPO.
# It contains PLACEHOLDERS. It must never contain a real hash or a real key.
#
# Generate the real answer file with:
#
#     installer/make-autoinstall.sh --ssh-key ~/.ssh/id_ed25519.pub
#
# which writes installer/autoinstall.yaml - gitignored, never committed.
#
# <<< TEMPLATE-ONLY <<<
#
# Retro Box unattended installer. JV Projects.
# Target: Ubuntu Server 26.04 LTS ("Resolute Raccoon") amd64.
#
# ---------------------------------------------------------------------------
# Four things in here are load-bearing. Do not "tidy" them.
#
#   1. There is NO `kernel:` section. On 26.04 the server installer now picks
#      an OEM/HWE kernel from detected hardware, and subiquity raises a fatal
#      AutoinstallError if an explicit kernel is ALSO requested. Adding
#      `kernel: {flavor: generic}` turns this into an install that hard-fails
#      on exactly the certified secondhand hardware this product targets.
#
#   2. There is NO `oem:` section. Its default is `auto`, and on 26.04 `auto`
#      means enabled for Server. That automatic kernel/driver selection is the
#      entire reason this product moved to 26.04. `oem: {install: false}` is
#      the one line that switches it off. Never add it.
#
#   3. Every entry in `storage.layout.match` carries a `size` key. The
#      presence of `size` (or `ssd`) is what activates subiquity's implicit
#      "skip disks that have an in-use partition" filter - which is what stops
#      the installer wiping the USB stick it booted from. A bare `match: {}`
#      has no such protection and can destroy the installer stick.
#
#   4. The generated config.yaml carries an explicit `channels:` list AS WELL
#      AS media_root + auto_channels. That is deliberate, see provision.sh.
# ---------------------------------------------------------------------------

autoinstall:
  version: 1

  locale: en_AU.UTF-8
  keyboard:
    layout: au

  # --- Who the box is -------------------------------------------------------
  # The hostname is load-bearing: mDNS follows the system hostname and
  # http://retrobox.local/ is the only address this product documents.
  #
  # Setting it here also fixes a real chroot bug: scripts/install.sh tries to
  # rename the box with hostnamectl, and hostnamectl (unlike systemctl) has no
  # chroot guard - inside the installer it would talk to the LIVE INSTALLER's
  # hostnamed and rename that instead of the target. It is guarded with
  # `|| echo` so it is harmless, but the rename only actually lands because
  # subiquity did it here first.
  #
  # The password is a crypt(3) SHA-512 hash, full of '$'. installer/lib/
  # render_autoinstall.py runs it through the same yaml_str() helper the wifi
  # SSID and password use below, which hands back a JSON (= valid YAML
  # double-quoted) scalar with correct escaping built in - so the placeholder
  # below carries its OWN quotes and must not be hand-quoted here. (A hash
  # happens to have no character that would break a hand-typed single quote;
  # an SSH key's comment field routinely does - see authorized-keys below.)
  # It is for LOCAL CONSOLE ACCESS ONLY - see `ssh:` below and installer/README.md.
  identity:
    realname: Retro Box
    hostname: __HOSTNAME__
    username: __USERNAME__
    password: __PASSWORD_HASH__

  # --- Remote access is key-only -------------------------------------------
  # allow-pw: false is the single line that makes a shared, publicly-known
  # console password safe. Someone standing at the box can log in. Someone on
  # the network cannot, whatever they know.
  #
  # The key, like the password hash above, is substituted already-quoted by
  # yaml_str() - NOT hand-quoted here. An SSH public key's comment field
  # routinely contains an apostrophe (e.g. "... jake's laptop"), and a
  # hand-typed single quote around that produces an answer file that is not
  # YAML. The unattended install then fails somewhere no one will ever see it.
  ssh:
    install-server: true
    allow-pw: false
    authorized-keys:
      - __SSH_AUTHORIZED_KEY__

  # --- Disk -----------------------------------------------------------------
  # Guided, whole-disk, no questions, unknown disk of unknown size.
  #
  # The ordered match list is tried top-down: prefer the largest internal SSD,
  # else the largest disk of any kind. Both carry `size`, so both inherit the
  # in-use-partition filter that excludes the stick we booted from.
  #
  # NOTHING HERE MAY NAME A SIZE. The target is a secondhand machine whose disk
  # is unknown until it is in your hands, so `largest` and `all` are the only
  # two quantities allowed in this section. Anything numeric turns "boots on
  # whatever we bought this week" into "boots on the one box it was written for".
  #
  # sizing-policy: all is the fix for a real, shipped fault. subiquity's default
  # is `scaled`, and scaled gives the root LV only PART of the volume group:
  # roughly half on a disk between 20 GB and 200 GB, and a flat 100 GiB above
  # that. The rest is left unallocated inside the VG, where nothing reports it
  # and nothing uses it. Measured on hardware: a 128 GB box came up claiming
  # 57 GB usable with 58 GB sitting idle. On a media appliance the size of the
  # disk is the one number the customer actually checks, so a 512 GB box that
  # says 240 GB reads as a lie, and the dashboard's System page repeats the
  # smaller figure without a murmur.
  #
  # `all` is the declarative fix and it is the primary one - it costs nothing,
  # it asks no questions, and it works on a disk of any size. It is not the only
  # one. installer/storage-grow.sh re-checks the finished box at the end of the
  # install, claims anything still unallocated, and shouts into
  # /var/log/retrobox-install.log if the root filesystem still does not account
  # for the disk. Two independent mechanisms, because this failure is silent and
  # the box is sold.
  storage:
    layout:
      name: lvm
      sizing-policy: all
      match:
        - ssd: true
          size: largest
        - size: largest

  # --- Network --------------------------------------------------------------
  # Wired and wireless both configured. Wired wins when present (lower route
  # metric); wireless is the automatic fallback.
  #
  # `optional: true` on every interface, including the ones in the wifi block
  # that render_autoinstall.py substitutes in below. (Do not write that
  # placeholder's name in a comment: the renderer replaces every occurrence of
  # it, so a second one lands a netplan block in the middle of a sentence and
  # the answer file stops being YAML. tests/test_installer_boot.py parses both
  # renders for exactly this reason.)
  #
  # netplan's default is `optional: false`, which it
  # renders as RequiredForOnline=yes, and one interface like that is all
  # systemd-networkd-wait-online needs to hold the boot up for its full timeout
  # on a box with nothing plugged in. This box must boot and play with no
  # network at all, so nothing here may ever block.
  #
  # It is NOT the whole fix, and this section must not be trusted as if it
  # were. `optional` only governs interfaces described HERE. The units that
  # actually drag network-online.target into the boot transaction are smbd and
  # wsdd, which scripts/setup_lan_share.sh installs and enables, and the
  # dashboard's Network page writes netplan files of its own at runtime that
  # this file has no say over. installer/harden-boot.sh masks the wait-online
  # units outright, which is the fix that does not depend on who wrote which
  # netplan file. Read the header of that script before touching either.
  network:
    version: 2
    ethernets:
      wired-en:
        match:
          name: "en*"
        dhcp4: true
        optional: true
        dhcp4-overrides:
          route-metric: 100
      wired-eth:
        match:
          name: "eth*"
        dhcp4: true
        optional: true
        dhcp4-overrides:
          route-metric: 100
__WIFI_BLOCK__
  # git is needed twice over: to clone the product below, and by the dashboard's
  # self-update, which shells out to git. scripts/install.sh never installs it.
  packages:
    - git
    - ca-certificates

  # --- Provisioning ---------------------------------------------------------
  # These run in the INSTALLER environment with the installed system mounted at
  # /target, so everything that touches the box is wrapped in
  # `curtin in-target --`. Any non-zero exit aborts the install loudly, which is
  # what we want: a box that fails visibly beats a box that boots black.
  #
  # All the real work happens here, at install time, NOT on first boot. That is
  # deliberate: the goal is to walk away and come back to a FINISHED box. A
  # first-boot installer would hand the customer a ten-minute apt+pip run, and
  # would need the internet at the customer's site instead of at the bench.
  #
  # The heavy lifting lives in installer/provision.sh, taken from the clone, so
  # it is version-controlled and shellcheck-able instead of being smuggled
  # through a YAML heredoc.
  late-commands:
    # 1. The clone. /opt/retrobox is a fixed absolute path so it does not move
    #    with the username. The self-updater derives its own location from
    #    where the package is imported so any path works - but it must be a real
    #    git clone whose remote is named origin, or `git fetch --tags` silently
    #    fetches nothing and every future update rolls itself back.
    - curtin in-target -- git clone https://github.com/vanticstudio/RetroBoxJVProjects.git /opt/retrobox

    # 2. Pin a release, so a new box starts on a known tag and the update system
    #    has a coherent starting point.
    #
    #    THIS ABORTS THE INSTALL IF THE REPO HAS NO vX.Y.Z TAG. That is the
    #    point: without one, every stick baked that day would produce a box
    #    running unreviewed HEAD, and these boxes are sold and then unreachable.
    #    The failure explains how to cut a release. Building a bench box on
    #    main is still possible - add --allow-unpinned - and it says loudly in
    #    the log that the result is not sellable.
    - curtin in-target -- /opt/retrobox/installer/pin-release.sh

    # 3. Everything else.
    - curtin in-target -- env RETROBOX_USER=__USERNAME__ RETROBOX_REPO_DIR=/opt/retrobox /opt/retrobox/installer/provision.sh

  # If anything above fails the install has already aborted. Copy our log out
  # to where a post-mortem will look for it. error-commands' own failures are
  # ignored by subiquity, so the `|| true` is belt and braces.
  error-commands:
    - cp /target/var/log/retrobox-install.log /var/log/installer/ 2>/dev/null || true

  # Leave the box off at the end so it can be unplugged from the bench and
  # carried to the TV. Change to `reboot` to have it come straight back up.
  shutdown: poweroff
